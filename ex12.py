base = 0
altura = 0
while base == 0:
    base = float(input("Qual a base: "))
while altura ==0:
    altura = float(input("Qual a altura: "))

area = (base * altura)

print(f"A área do retangulo é {area}")
